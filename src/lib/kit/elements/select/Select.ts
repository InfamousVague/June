import { Shape, SimpleApperance } from "$lib/types/Appearance.js";
import { BinaryState } from "$lib/types/State.js";

export const defaultShape: Shape = Shape.Pill;
export const defaultAppearance: SimpleApperance = SimpleApperance.Dark;
export const defaultState: BinaryState = BinaryState.On;