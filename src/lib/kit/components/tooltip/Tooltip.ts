import { Position } from "$lib/types/Position.js";

export const defaultContent: string = "tooltip_text";
export const defaultPosition: Position = Position.Top;
export const defaultOffset: number = 8;